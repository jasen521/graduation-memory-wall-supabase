# 常见问题排查

## 1. 上传时报错：请先在 config.js 填写 Supabase Project URL

说明 `config.js` 没改。

解决：

打开 `config.js`，填写：

```js
supabaseUrl: "https://你的项目ID.supabase.co",
supabaseAnonKey: "你的 anon public key"
```

---

## 2. 上传时报错：new row violates row-level security policy

原因：

```text
photos 表 RLS 权限没有开好。
```

解决：

重新执行 `supabase_setup.sql`。

重点是这段必须成功：

```sql
create policy "Allow anon insert photos"
on public.photos
for insert
to anon
with check (...);
```

---

## 3. 上传时报错：storage unauthorized

原因：

```text
Storage 上传权限没开，或 bucket 名称不一致。
```

检查：

```text
config.js 里的 bucketName 是否是 graduation-photos
Supabase Storage 是否有 graduation-photos
```

解决：

重新执行 `supabase_setup.sql`。

---

## 4. 上传成功，但大屏没有反应

按顺序检查：

### A. 大屏是不是打开了正确页面

正确：

```text
https://你的项目.vercel.app/screen.html
```

不是：

```text
https://你的项目.vercel.app/
```

根路径是上传页。

### B. Supabase photos 表里有没有新增数据

进入：

```text
Supabase → Table Editor → photos
```

如果没有新增，说明上传端没有成功插入数据库。

### C. Realtime 是否开启

重新执行：

```sql
do $$
begin
  alter publication supabase_realtime add table public.photos;
exception
  when duplicate_object then null;
end $$;
```

然后刷新大屏页面。

---

## 5. 大屏有记录，但图片不显示

原因通常是 Storage bucket 不是 public。

检查：

```text
Supabase → Storage → graduation-photos
```

确认 bucket 是 public。

如果不是，重新执行 `supabase_setup.sql`。

---

## 6. 二维码扫进去是大屏，不是上传页

二维码链接填错了。

正确二维码内容：

```text
https://你的项目.vercel.app/
```

错误二维码内容：

```text
https://你的项目.vercel.app/screen.html
```

---

## 7. Vercel 页面空白

打开浏览器控制台，看是否有 404。

重点检查 HTML 里的路径：

```html
<link rel="stylesheet" href="./css/style.css" />
<script src="./config.js"></script>
<script src="./js/upload.js"></script>
<script src="./js/screen.js"></script>
```

如果你改变了目录结构，路径也要跟着改。

---

## 8. 上传照片太慢

建议：

```text
1. 不要上传特别大的原图
2. 手机端代码已经会自动压缩大图
3. Supabase 免费版网络有时会慢，正式活动前要提前测试
```

---

## 9. 想关闭无审核直接上传怎么办

当前版本是无后台审核，符合“扫码直接上传、成功即上墙”。

如果后续想加审核，可以在 photos 表新增字段：

```sql
approved boolean default false
```

然后大屏只读取：

```js
.eq("approved", true)
```

但这个版本没有启用审核。

---

## 10. 想清空所有照片重新开始

Supabase SQL Editor 执行：

```sql
delete from public.photos;
```

然后去 Storage 里手动删除 `graduation-photos/uploads` 下的文件。

---

## 11. 大屏照片球偏了

这版已经修正居中逻辑。

照片球容器是：

```css
.sphere-stage {
  left: 50%;
  top: 52%;
  transform: translate(-50%, -50%);
}
```

如果你觉得偏高或偏低，改：

```css
top: 52%;
```

例如：

```css
top: 50%;
```

---

## 12. 背景建筑太明显，抢照片主体

打开 `css/style.css`，搜索：

```css
.csust-landmark-bubble
```

把：

```css
opacity: .20;
```

改小，例如：

```css
opacity: .12;
```

---

## 13. 大屏二维码不显示

二维码依赖在线 CDN：

```html
https://cdn.jsdelivr.net/npm/qrcodejs@1.0.0/qrcode.min.js
```

如果现场网络拦截 CDN，大屏会显示上传链接文字。你也可以提前生成二维码图片，直接替换大屏二维码区域。

---

## 14. 正式活动前必须测试的三件事

```text
1. 手机能打开上传页
2. 上传照片后 Supabase photos 表新增数据
3. 大屏 screen.html 能实时弹出照片
```

这三步通了，现场就能跑。
